<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.labs.tatu.runclub">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.labs.tatu.runclub.MainActivity"
            android:screenOrientation="portrait">

        </activity>
        <activity
            android:name="com.labs.tatu.runclub.EventsActivity"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name="com.labs.tatu.runclub.InboxActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.labs.tatu.runclub.FriendsActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.labs.tatu.runclub.StatsActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.labs.tatu.runclub.AddLocationActivity"
            android:screenOrientation="portrait" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="com.labs.tatu.runclub.MapsActivity"
            android:label="@string/title_activity_maps"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.labs.tatu.runclub.LocationRunActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.labs.tatu.runclub.SplashScreenActivity"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.labs.tatu.runclub.AddEventActivity" />
        <activity
            android:name="com.labs.tatu.runclub.SingleEventActivity"
            android:parentActivityName="com.labs.tatu.runclub.EventsActivity">
            >
        </activity>
        <activity android:name="com.labs.tatu.runclub.WorkoutActivity"
            android:parentActivityName="com.labs.tatu.runclub.MainActivity"
            ></activity>
    </application>

</manifest>